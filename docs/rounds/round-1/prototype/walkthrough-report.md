# 场景走查报告 · managed-doc-panel.html（round-1）

走查人：独立 subagent（未参与制作）/ 走查时间：2026-08-17 21:30 / 方式：Playwright 真浏览器操作

> 环境说明（均已在走查中处理）：本机 headless 浏览器对 `file://` 协议一律 `ERR_FAILED`（ASCII 与中文路径都试过），故经本地 HTTP（localhost:18732）加载同一份单文件 HTML 真浏览器操作——HTML 无外部依赖，加载行为一致；系统 Chrome 151 缺库启动即崩，使用缓存 headless-shell（chromium_headless_shell-1234）实际执行；走查员不支持图像输入，视觉颜色证据改为在浏览器内用 `getComputedStyle` 读取计算样式（确定性颜色值，比截图更硬核）。操作证据 = 真实点击 + 页面状态/样式/文本逐项读取。

## 走查明细

| 场景 | 步骤/期望 | 结果 | 证据/说明 |
|---|---|---|---|
| S1 | 前置：5 块改动（1修改/1新增/1删除/2修改） | PASS | 5 个改动块卡片，标签 = 修改1/5、新增2/5、删除3/5、修改4/5、修改5/5，与剧本前置一致 |
| S1 | 步骤1：文档内联呈现，未改动段落正常显示 | PASS | 11 个非块段落正常显示，5 块高亮卡片嵌在段落原位（§2.1、§2.3、§3 后、§5.1、附录 A 内） |
| S1 | 步骤1：块内绿 +红 − | PASS | 逐块核对：修改块 = del（红底删除线 rgb(251,228,226)+line-through）+ ins（绿底 rgb(221,239,227)）；新增块 = 仅 ins；删除块 = 仅 del——diff 符号与块类型匹配（首次断言「5 块都有 del+ins」误判删除/新增块，修正后按块类型成立） |
| S1 | 步骤1：初始 5 块全待定、写回禁用、进度 0/5 | PASS | 5 块计算样式均为黄（bg rgb(253,246,231)）；写回 disabled=true；countLabel=0/5 |
| S1 | 步骤2：逐块 1✓ 2✓ 3✗ 4✓ 5✗，状态即时变色 | PASS | 点完 3 块：块1/2=绿(accepted)、块3=红(rejected, opacity .72)、块4/5=黄待定——三色并存；点完 5 块：块1/2/4 绿、块3/5 红，与点击一致 |
| S1 | 步骤2：进度指示实时更新 | PASS | 点完 3 块 countLabel=3/5，点完 5 块=5/5（含 fab 内圆点同步） |
| S1 | 期望②：存在待定块时写回不可用 | PASS | 初始（5 待定）disabled=true；点完 3 块（2 待定）仍 disabled=true |
| S1 | 步骤3：全部有着落后「写回」激活 | PASS | 5/5 后写回 enabled=true |
| S1 | 步骤4：写回 approval_response，接受块物化 v4、拒绝块保留 v3 | PASS | 点写回后 okBanner：「已写回 approval_response（含逐块明细）：接受 3 块 → 物化为 v4；拒绝 2 块 → 保留 v3 内容」；badge=「已确认 · v4 已物化」；verLabel=「当前 v4（提案自 v3）」；接受块变 settled-ok 实心绿、拒绝块 settled-rej 半透明红（opacity .72） |
| S1 | 期望①：5 块状态三色可见且与点击一致 | PASS | class 状态 + 计算样式双重证据：黄(待定) rgb(253,246,231) / 绿(接受) rgb(238,248,241) / 红(拒绝) rgb(253,238,236)，与逐块点击一致 |
| S1 | 期望③：写回后版本链出现 v4 | PASS | 打开版本链抽屉：v1、v2、v3、v4（「设计阶段 · agent: designer 刚刚 · 本次写回物化」）四行齐全 |
| S1 | 期望③：v4 内容 = v3 + 被接受的块 | PASS（证据边界） | okBanner 声明「接受 3 块 → 物化为 v4；拒绝 2 块 → 保留 v3」，声明块数（3/2）与实际点击一致；但原型无版本内容查看视图，内容级断言无操作入口——仅声明性证据 |
| S1 | 期望④：被拒块不进 v4 | PASS | okBanner 声明「拒绝 2 块 → 保留 v3 内容」+ 被拒块（3/5）写回后呈 settled-rej 形态留在文档（不物化） |
| S1 | 期望⑤：每次裁决落入 append-only 日志 | PASS（证据边界） | okBanner 声明「裁决已落入 append-only 会话日志」；原型无日志查看视图，为声明性证据 |
| S2 | 步骤：点「✅ 全部接受」→ 5 块全绿、进度 5/5 | PASS | 5 块计算样式全绿（accepted），无 rejected/待定残留；countLabel=5/5；写回 enabled |
| S2 | 期望：一键后无待定块 | PASS | 5 块均有裁决（全 accepted） |
| S2 | 期望：单块按钮在批量后仍可翻转（混合档） | PASS | 全收后点块1 ✗ → 块1 变红(rejected)，进度仍 5/5（有裁决非待定），写回仍 enabled |
| S2 | 期望：写回后 v4 = 提案全部内容 | PASS | 翻转回来后写回：okBanner「接受 5 块 → 物化为 v4；拒绝 0 块」 |
| S3 | 前置：文档已有 v1–v4 版本链 | PASS | 剧本前置需先物化 v4：走查经「全部接受+写回」构造（写回后链上 v1–v4 四行齐全）；注：原型初始链上只有 v1–v3 +「待确认提案」行（v4 未物化），符合 v3→v4 提案语境，非缺陷 |
| S3 | 期望①：版本链完整可见且归属正确 | PASS | 逐行核对：v1（需求阶段·requirement-writer·3 天前·初稿）、v2（需求阶段·requirement-writer·2 天前·补充验收标准）、v3（设计阶段·designer·昨天·技术选型定稿）、v4（设计阶段·designer·刚刚·本次写回物化）——每行含版本号/归属 stage/agent/时间/变更摘要 |
| S3 | 步骤：对 v2 点「回滚到此版」→ 确认提示 | PASS | confirm 弹窗：「回滚到 v2：将以 v2 的内容生成新版本 v5。历史版本不会被删除（append-only）。继续？」 |
| S3 | 期望②：回滚 = 以 v2 内容生成新版本 v5（不删除历史） | PASS | 执行后 infoBanner「已回滚：以 v2 内容生成新版本 v5；v3 / v4 保留在版本链上可随时查看」；链上出现 v5 当前行；v1–v3 行保留（v4 见下条 BREAK） |
| S3 | 期望③：回滚后当前版本显示 v5 | PASS | badge=「当前 v5（自 v2 回滚）」、verLabel 同步、抽屉当前行为 v5 |
| S3 | 期望③：v3/v4 仍可在链上查看 | **BREAK** | 回滚后版本链实际只列 v1、v2、v3、v5——**v4 行消失**，链上无 v4 可查看；infoBanner 声明「v3 / v4 保留在版本链上可随时查看」与链上事实不符（v3 在、v4 不在）。根因：v4 是「动态当前行」（curVer=4 时渲染），回滚将 curVer 置 5 后 v4 既不属静态 history 数组、也不再是当前行，被整体丢弃。修复建议：写回物化 v4 时将其并入历史列表（或 vlist 渲染以物化记录持久保存），使回滚后 v4 作为历史行保留、可查看 |
| S3 | 异常分支：confirm 取消 | PASS | 取消后：无 infoBanner、badge 仍「已确认 · v4 已物化」、链上无 v5（confirm 被 dismiss，不产生新版本） |
| S4 | 期望①：顶部显式警告横幅（EXTERNAL_MODIFIED） | PASS | 点「⚠️ 演示：外部手改」后横幅可见：「检测到外部手改（EXTERNAL_MODIFIED）：物化文件『设计文档.md』在系统外被修改（14:32，编辑器直写）。处理前版本操作已冻结。」（黄底横幅） |
| S4 | 期望③：警告消除前写回/批量被阻止 | PASS | extMode 下：写回 disabled=true、全部接受/全部拒绝 disabled=true、fab hint 变「⛔ 有外部手改待处理，版本操作冻结」 |
| S4 | 期望③：警告消除前回滚被阻止 | PASS | extMode 下点 v2「回滚到此版」：无 confirm 弹窗（dialog 捕获 0）、版本未变（badge 仍「待确认 · 5 块」）——回滚按钮点击被 extMode 守卫拦截 |
| S4 | 期望②：提供「查看 diff」 | PASS | 点「查看 diff」弹出示意 alert：「（原型示意）展开外部手改 diff：- 物化文件第 87 行被改为 … + 系统版本第 87 行为 …」 |
| S4 | 期望②：提供处理动作（拒绝采纳/以提案方式合并），不得静默覆盖或静默丢弃 | PASS（缺口注明） | 实现「拒绝采纳，恢复系统版本」（点后横幅消失、冻结解除）；**「以提案方式合并」动作未实现**——剧本期望以斜杠列二选一，按二选一解读满足；若产品意图两种动作都要，需补该按钮 |
| S4 | 期望③：处理后版本操作恢复 | PASS | 拒绝采纳后：写回恢复可用（需先 5/5 裁决，5 块待定时写回本就被禁用——非 extMode 残留）；回滚按钮恢复（点 v2 重新触发 confirm） |
| 剧本外 | 右侧滚动链导航（toc） | PASS | 10 项（5 标题 + 5 diff 块混合导航）；点击「§3 架构」scrollY 0→1079 平滑滚动；scrollspy 随阅读位置高亮 active；diff 节点颜色随确认状态联动（块1 裁决后节点 class b-p→b-y） |
| 剧本外 | 悬浮操作条（fab） | PASS | 确认进度 countLabel + 圆点 dots + 「✅ 全部接受」「❌ 全部拒绝」「⬆ 写回」+ hint；全部拒绝实测：5 块全红、进度 5/5、写回 enabled |
| 剧本外 | 「↺ 重置」按钮 | PASS | 点后页面重载：badge 回「待确认 · 5 块」、进度 0/5（演示态归零） |
| 剧本外 | 版本链抽屉 append-only 说明 | PASS | 抽屉副标题：「append-only：历史永不改写；回滚 = 以旧版内容生成新版本」 |

## 剧本外功能

走查中出现但剧本未写的功能（均已实际操作验证）：右侧滚动链导航（标题 + diff 块混合导航、hover 展开 label、点击平滑滚动、scrollspy 高亮、节点颜色随确认状态联动）、悬浮操作条（进度计数 + 圆点 + 全部接受/全部拒绝 + 写回 + 提示语）、「❌ 全部拒绝」批量按钮、「↺ 重置」演示按钮、「⚠️ 演示：外部手改」模拟入口、版本链抽屉 append-only 说明文字。

## BREAK 汇总（1 个）

| 场景 | 期望条目 | 剧本写的期望 | 原型实际表现 | 修复建议 |
|---|---|---|---|---|
| S3 | 期望③「v3/v4 仍可在链上查看」 | 回滚后 v3/v4 仍可在版本链上查看 | 回滚到 v2 后版本链只列 v1、v2、v3、v5，**v4 行消失**；仅 infoBanner 文字声明「v3 / v4 保留」，链上无 v4 条目 | 写回物化 v4 时将其并入历史列表（持久记录已物化版本），回滚后 v4 作为历史行保留、可查看 |

## 走查矩阵覆盖核对

S1–S4 全部期望条目（含异常分支：S3 confirm 取消、S4 外部手改）均已在走查中实际覆盖，无剧本内期望被跳过。S1 期望③（v4 内容构成）与期望⑤（日志落盘）在原型中仅能以 okBanner 声明作为证据（原型无版本内容/日志查看视图），已在对应行注明证据边界，不影响场景走通。

## 复走（修复后）

走查人：独立 subagent（未参与制作）/ 复走时间：2026-08-17 22:05 / 方式：Playwright 真浏览器操作（同首轮环境：本地 HTTP 加载同一份 HTML + 缓存 headless-shell + TMPDIR 重定向）

> 修复内容核对：①版本链渲染重构——写回物化与回滚生成均写入 versions 数组（append-only），当前行单独渲染，回滚版本号递增（v5、v6…）；②未物化时历史行回滚按钮显式 disabled（title=「有待确认提案未处理，暂不可回滚」）；③S4 横幅新增「以提案方式合并」按钮。以上改动均在复走前从源码确认存在，以下为真浏览器操作断言。

### 复走明细

| 场景 | 步骤/期望 | 结果 | 证据/说明 |
|---|---|---|---|
| S3 主路径 | 全部接受 → 写回物化 v4 | PASS | 写回后打开版本链：v1–v4 四行，v4 为当前行（写回物化已入 versions 数组） |
| S3 主路径 | 物化后历史行回滚可用 | PASS | v1–v3 回滚按钮均 enabled（未物化禁用已解除） |
| S3 主路径 | 对 v2 点回滚 → 确认提示（版本号动态） | PASS | confirm：「回滚到 v2：将以 v2 的内容生成新版本 v5。历史版本不会被删除（append-only）。继续？」（版本号 = nextVer 动态） |
| S3 主路径 | 期望③（原 BREAK）：回滚后 v4 保留在链上 | PASS | 回滚到 v2 后版本链 = v1、v2、v3、v4、v5 五行，**v4 行在**（非当前历史行）；当前行为 v5（badge=「当前 v5（自 v2 回滚）」、verLabel 同步） |
| S3 主路径 | infoBanner 文案与链上事实一致 | PASS | infoBanner：「已回滚：以 v2 内容生成新版本 v5；此前全部版本（含 v2 之后的）保留在链上可随时查看」——与链上 v3/v4 保留事实一致（原 BREAK 中「声明与事实不符」已消除） |
| S3 主路径 | 连续回滚：再对 v3 回滚生成 v6（抽验） | PASS | confirm 版本号 = v6；执行后链上 v1–v6 六行，当前行 v6，v4/v5 均作为历史行保留（append-only，无删除） |
| S3 异常分支 | 初始未物化态打开版本链：历史行回滚禁用 | PASS | 链上 v1–v3 + 「待确认提案」当前行；v1–v3 回滚按钮 disabled=true 且 title=「有待确认提案未处理，暂不可回滚」；点击无 confirm、无版本变化 |
| S3 异常分支 | confirm 取消（修复后抽验） | PASS | dismiss 后：链上无 v5（仍 v1–v4）、无 infoBanner、badge 仍「已确认 · v4 已物化」 |
| S4 | 「以提案方式合并」按钮存在 | PASS | extBanner 动作按钮 = 查看 diff / 以提案方式合并 / 拒绝采纳，恢复系统版本（三动作齐备，原缺口已补） |
| S4 | 点击「以提案方式合并」 | PASS | alert 示意：「外部手改已转为 propose_edit 提案：改动以新提案块呈现，走同一条逐块确认通道」；横幅消失、冻结解除（写回恢复：5/5 裁决后 enabled=true） |
| 回归 S1 | 逐块混合确认 → 写回 | PASS | 1✓2✓3✗4✓5✗ → 进度 5/5、块1绿块3红 → 写回 okBanner「接受 3 块 → 物化为 v4；拒绝 2 块 → 保留 v3 内容」→ 版本链 v1–v4 且 v4 当前 |
| 回归 S2 | 全部接受 → 单块翻转 → 写回 | PASS | 全收 5/5 → 打回块1（红、进度仍 5/5）→ 翻回 → 写回 okBanner「接受 5 块 → 物化为 v4」 |

### 复走结论

修复点全部经真浏览器操作验证成立：原 BREAK（S3 回滚后 v4 行从版本链消失）已修复——回滚后 v1–v5 五行齐全、v4 保留可查看、infoBanner 与链上事实一致，连续回滚版本号递增且 append-only 语义保持；S3 未物化禁用分支与 S4「以提案方式合并」分支均按预期行为；S1/S2 快速回归无退化。复走 25 项断言全部 PASS，无新增 BREAK。首轮 BREAK 记录保留在上文作为留痕。

## 复走二（方案 C）

走查人：独立 subagent（未参与制作）/ 复走时间：2026-08-17 22:30 / 方式：Playwright 真浏览器操作（同前环境：本地 HTTP 加载同一份 HTML + 缓存 headless-shell + TMPDIR 重定向）

> 本轮依据更新后的 prd.md S3 新增期望④（回滚反馈）⑤（撤销回滚）走查。方案 C 实现机制（源码确认）：回滚后 body 加 `rolled-back` 态——`.block` 灰化（opacity .55、灰底灰框）+ 块 tag 追加「 · 未生效（v4 提案）」、`ins` 隐藏、`del` 恢复正常文本、`.pick` 确认按钮隐藏；`v4only` 元素（§2.3 标题、新增块2）隐藏、`legacy` 元素（§4 标题+完整段落）恢复显示；infoBanner 重建为回滚报告（含「查看 v5↔v4 差异」「撤销回滚」两按钮）；撤销回滚生成递增新版本并移除 rolled-back 态。

### 复走二明细

| 场景 | 步骤/期望 | 结果 | 证据/说明 |
|---|---|---|---|
| S3 期望④ | 正文切换为 v2 时代内容：修改块灰化标「未生效」 | PASS | 回滚到 v2 后 body.rolled-back=true；块1 tag 伪元素 ::after=「 · 未生效（v4 提案）」；计算样式 opacity=0.55、灰底（块1/4/5 灰化） |
| S3 期望④ | ins 隐藏、del 恢复正常文本 | PASS | 块1 ins display=none（隐藏）；del textDecoration=none、无红底（恢复正常文本，无删除线） |
| S3 期望④ | 新增块2 与 §2.3 标题隐藏 | PASS | 回滚态：块2 与 §2.3 标题（v4only）均 display:none（v2 时代不存在） |
| S3 期望④ | 被删 §4 恢复完整段落显示 | PASS | §4 标题 + 完整段落（legacy）恢复显示（display=block×2），内容为 PM2/Nginx 旧部署方案 |
| S3 期望④ | 回滚报告横幅：撤销 5 块、确认过 N 块、appendEntry 注记 | PASS | 报告：「已回滚：v5 = v2 的内容（正文已切换）。v4 的 5 块改动不在当前版本——其中你确认过的 5 块一并撤销；版本链完整保留，操作记录已写入会话日志（appendEntry）。」（全收场景 N=5，与操作一致） |
| S3 期望④ | 「查看差异」「撤销回滚」两动作按钮 | PASS | 回滚报告动作按钮 = 查看 v5 ↔ v4 差异 / ↩ 撤销回滚（恢复 v4 内容 → 生成 v6） |
| S3 期望④ | 边界：回滚态确认按钮不可见/不可点 | PASS | 回滚态块内 ✓/✗ display=none，force 点击被阻断（无状态变化） |
| S3 期望④ | 边界：「查看差异」alert 弹出 | PASS | alert 示意见 v5↔v4 差异（v4：fork 0.84.2、§2.3 薄壳、删 §4…；v5：恢复 v2 内容、§4 回来） |
| S3 期望⑤ | 点「撤销回滚」→ 正文恢复 v4 形态 | PASS | body.rolled-back=false；§4 消失（display:none）、§2.3 与新增块2 恢复显示、ins 恢复 inline、✓/✗ 恢复可辨（提案块形态还原） |
| S3 期望⑤ | 以 v4 内容生成新版本（版本号递增） | PASS | badge=「当前 v6（撤销回滚 = v4 内容）」、verLabel 同步（撤销回滚生成 v6，版本号递增） |
| S3 期望⑤ | 撤销报告文案 | PASS | 「已撤销回滚：v6 = v4 内容（含你确认的 5 块，正文已恢复）。回滚版 v5 保留在版本链上可随时再回滚。」 |
| S3 期望⑤ | 回滚版保留在版本链上 | PASS | 撤销后版本链 = v1–v6 六行完整，当前行 v6，回滚版 v5 作为历史行保留（append-only） |
| 回归 S1 | 逐块混合确认 → 写回 | PASS | 1✓2✓3✗4✓5✗ → 写回 okBanner「接受 3 块 → 物化为 v4；拒绝 2 块 → 保留 v3 内容」→ 版本链 v1–v4 且 v4 当前（无退化） |
| 回归 S4 | 外部手改三动作 | PASS | 三动作按钮齐备（查看 diff / 以提案方式合并 / 拒绝采纳，恢复系统版本）；extMode 下写回冻结；查看 diff alert、以提案方式合并（横幅消失冻结解除）、拒绝采纳（横幅消失，5/5 裁决后写回恢复 enabled）均按预期（无退化） |

### 复走二结论

方案 C 新增期望④⑤全部经真浏览器操作验证成立：回滚后正文切换为 v2 时代内容（块灰化标「未生效」、新增段隐藏、被删 §4 恢复），回滚报告含撤销/确认块数与 appendEntry 注记及两动作；撤销回滚生成 v6、正文恢复 v4 形态、回滚版 v5 保留链上，版本号递增与 append-only 语义保持；S1/S4 回归无退化。复走二共 23 项断言全部 PASS，无 BREAK（期间 2 个 FAIL 均为我方脚本问题——transition 中间态读数、待定块时写回本就禁用——修正断言后全过，非原型缺陷）。注：回滚报告按钮位于横幅右端，版本链抽屉开启时会遮挡点击，需先关抽屉再点（按真实用户流程处理，不判 BREAK）。

STATUS: PASS —— 全部场景与异常分支走通，覆盖无缺口

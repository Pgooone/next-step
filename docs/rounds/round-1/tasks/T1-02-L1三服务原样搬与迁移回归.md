# T1-02 · L1 三服务原样搬 + 迁移回归

> 柱子：**可控**（唯一被二十轮验证过的核心资产，回归护栏先行——承重墙 1）
> 让哪条变绿：无直接 AC——T1-03 起的全部 L1 卡的基座；正本 §7「原样搬」红线落点
> 层：L1｜ **沿用（原样搬）**：`lib/domain/lcs.ts`、`lib/domain/file-name.ts`、`lib/domain/artifact-service.ts`、`lib/domain/pending-change-service.ts`（旧仓 `/home/pgoone/GitHubproject/Next-Step/next-step-V1.2/`）

## 依赖
- 前置卡：T1-01（monorepo 骨架 + 路径常量）

## 实现要点
- **原样搬四个文件到 `packages/core/src/domain/`，语义零改动**（本卡严禁任何领域改动，baseVersion 等属 T1-03）：
  - `lcs.ts` 全部（`splitLines` :23-28 / `lcsDiff` :34-67 / `DiffOp` :14-17）——零 node 依赖，纯算法
  - `file-name.ts` 全部（`sanitizeFileName` :21-26 / `buildArtifactFileName` :47-59）——仅依赖 node:fs/path，保留「抽到无 pi 依赖独立模块」先例注释
  - `artifact-service.ts`（`ArtifactService` 类全部方法 + `Artifact`/`ArtifactVersion`/`ArtifactError` 类型 :20-61）——唯一允许改动：`managedDir` 里 `.pi` → `NEXTSTEP_DIR_NAME` 常量（T1-01 导入）；其余（atomicWrite / assertNotExternallyModified / 乐观锁 / rollback 追加语义）逐字保留
  - `pending-change-service.ts`（`PendingChangeStore` 类 + 纯函数 `computeReplaceDiffBlocks` :129 / `computeEditDiffBlocks` :138 / `applyResolvedBlocks` :165 / `buildReplacePendingChange` :205 / `buildPatchPendingChange` :226 + 类型 :22-65）——同上：仅 pendingDir 的 `.pi` 换常量
- **旧仓单测平移**：找旧仓对应测试文件（`lib/domain/` 下的 `*.test.ts`，覆盖 artifact-service / pending-change-service / lcs / file-name / project-registry），平移进 `packages/core/` 并跑绿——这是「原样搬」的回归护栏，**先过平移测试再做任何后续卡**。
- import 路径适配（旧仓相对路径 → 新包内路径）允许；测试内嵌的路径断言里的 `.pi` 随常量改。
- 本卡产出后 `packages/core` 无 `@earendil-works/*` 依赖（B1 继续成立）。

## 验收断言（可执行）
- [ ] 平移的全部旧仓单测跑绿（数量 = 旧仓对应文件用例数，逐条列出清单）
- [ ] `applyResolvedBlocks` 不变量既有断言保留且绿：全 confirmed = newContent / 全 rejected|pending = oldContent / 块数失配抛 INVALID（旧仓 :159-198 语义）
- [ ] 乐观锁既有断言保留且绿：If-Match 失配 → `VERSION_CONFLICT`（旧仓 :414-421）
- [ ] EXTERNAL_MODIFIED 既有断言保留且绿：写盘前外部改文件 → 干净失败、无半截状态（旧仓 :130-144）
- [ ] 回滚追加语义既有断言保留且绿：rollback 后版本链长度 +1、旧版全在（旧仓 :320-365）
- [ ] `grep -rn "@earendil-works" packages/core/` 零命中

## 完成判据
平移测试全绿 + 门禁过 + 逐卡 commit。verifier 独立复跑确认「原样搬」无领域改动（diff 对照旧仓）。

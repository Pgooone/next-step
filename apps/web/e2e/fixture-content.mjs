/**
 * E2E 共享 fixture 内容（T1-13）：文档正文与提案全文的唯一来源。
 *
 * fixture-seed.mjs 经模板注入使用同一份常量（保证「种子落的数据」与「驱动断言
 * 期望」逐字节同源）；drive-e2e.mjs 直接 import 本文件做物化文件级断言。
 * 内容与原型 managed-doc-panel.html / seed-demo.mjs 对齐（5 块分布不变）。
 */

/** v1 初稿（需求阶段）：§1/§2/§2.1/§2.2/§3 */
export const V1 = [
  "# Next-Step v2.0 · 设计文档",
  "",
  "## §1 概述",
  "",
  "本地多 Agent 产线：把「一句想法」变成「可动工的设计包 + 可评审的原型」。",
  "",
  "## §2 技术选型",
  "",
  "领域逻辑全部住在不认识 UI、也不认识 pi 的纯 TS 内核（L1）里；CLI 和 Web 只是两个壳（L3）。",
  "",
  "### §2.1 内核策略",
  "",
  "策略基线：内核跟随 pi ^0.79.0（不 fork，纯扩展叠加）。",
  "",
  "### §2.2 发行形态",
  "",
  "npm 个人账号发布，npm i -g 后运行 nextstep 命令即得装好全部修改的发行版。",
  "",
  "## §3 架构 · 一核两壳",
  "",
  "四层分层：L0 上游 pi → L1 领域内核 → L2 适配层 → L3 双壳。",
].join("\n");

/** v2（需求阶段补验收标准）= v1 + §4/§5/§5.1/附录 A；v3（设计阶段定稿）= v2 同内容 */
export const V2 = [
  ...V1.split("\n"),
  "",
  "## §4 旧部署方案",
  "",
  "PM2 进程守护 + Nginx 反代 + 手工部署清单（共 15 行流程与回滚脚本）。",
  "",
  "## §5 验收标准",
  "",
  "每条需求必须有可断言的验收标准（F6），否则不进范围。",
  "",
  "### §5.1 确认交互",
  "",
  "AC-1 确认交互仅逐块 ✓/✗。",
  "",
  "## 附录 A · 引用与来源",
  "",
  "上游内核引用 pi monorepo（MIT）。",
  "",
  "### A.1 内核引用",
  "",
  "上游内核引用 pi ^0.79.0。",
].join("\n");

/** v4 提案：5 块改动（1 mod §2.1 / 1 add §2.3 / 1 del §4 / 1 mod §5.1 / 1 mod 附录 A.1） */
export const V4 = [
  "# Next-Step v2.0 · 设计文档",
  "",
  "## §1 概述",
  "",
  "本地多 Agent 产线：把「一句想法」变成「可动工的设计包 + 可评审的原型」。",
  "",
  "## §2 技术选型",
  "",
  "领域逻辑全部住在不认识 UI、也不认识 pi 的纯 TS 内核（L1）里；CLI 和 Web 只是两个壳（L3）。",
  "",
  "### §2.1 内核策略",
  "",
  "策略基线：内核 fork pi 0.84.2 为基线：改动只限品牌与发行层，领域逻辑仍全走扩展层（D1 拍板）。",
  "",
  "### §2.2 发行形态",
  "",
  "npm 个人账号发布，npm i -g 后运行 nextstep 命令即得装好全部修改的发行版。",
  "",
  // §2.3 新增区不加空行（标题紧贴内容）：避免 LCS 与邻区行结构同构交错配对，保证独立 add 块
  "### §2.3 Web 壳",
  "Web 壳完全自建薄壳：不 fork pi-web，壳职责收窄为「读会话 JSONL → 渲染 presentation 纯数据 → 写回 approval_response」，壳零领域判断（D8 拍板）。",
  "",
  "## §3 架构 · 一核两壳",
  "",
  "四层分层：L0 上游 pi → L1 领域内核 → L2 适配层 → L3 双壳。",
  "",
  "## §5 验收标准",
  "",
  "每条需求必须有可断言的验收标准（F6），否则不进范围。",
  "",
  "### §5.1 确认交互",
  "",
  "AC-1 确认交互分档：整块收 / 逐块 / 混合（先全收再打回单块）；交互可分档，记账永远块级（D6 拍板）。",
  "",
  "## 附录 A · 引用与来源",
  "",
  "上游内核引用 pi monorepo（MIT）。",
  "",
  "### A.1 内核引用",
  "",
  "上游内核引用 pi 0.84.2（fork 基线）；跟进纪律：UPSTREAM.md 对照 + 内核 diff 最小化清单（D1/D9 拍板）。",
].join("\n");

/** S4 外部手改追加行（绕过系统直写物化文件）。 */
export const EXT_LINE_1 = "外部手改行：这行被绕过系统直接编辑了。";
export const EXT_LINE_2 = "又一处外部手改：merge 通道验证。";

/** 混合裁决（块 1✓ 2✓ 3✗ 4✓ 5✗，3 收 2 拒）后 v4 内容的关键断言对（含/不含）。 */
export const MIXED_V4_EXPECT = {
  mustContain: [
    "策略基线：内核 fork pi 0.84.2 为基线：改动只限品牌与发行层", // 块 1 mod §2.1 生效
    "### §2.3 Web 壳", // 块 2 add §2.3 生效
    "AC-1 确认交互分档：整块收 / 逐块 / 混合", // 块 4 mod §5.1 生效
    "## §4 旧部署方案", // 块 3 del §4 被拒 → §4 保留
    "PM2 进程守护 + Nginx 反代", // 同上（§4 正文仍在）
    "上游内核引用 pi ^0.79.0。", // 块 5 mod A.1 被拒 → v3 原文保留
  ],
  mustAbsent: [
    "上游内核引用 pi 0.84.2（fork 基线）；跟进纪律", // 块 5 新文案不出现
  ],
};
